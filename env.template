# Note Taker AI - Environment Configuration
# Copy this file to .env and fill in your API keys

# ============================================
# Required Configuration
# ============================================

# AssemblyAI API Key (Required)
# Get your key at: https://www.assemblyai.com/
ASSEMBLYAI_API_KEY=your_assemblyai_key_here

# OpenAI API Key (Required)
# Get your key at: https://platform.openai.com/
OPENAI_API_KEY=your_openai_key_here

# ============================================
# Optional Configuration
# ============================================

# OpenAI Model (defaults to gpt-4o-mini if not set)
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# Flask Environment (defaults to development if not set)
# Options: development, production
FLASK_ENV=development

# Flask App Entry Point
FLASK_APP=app.py

# ============================================
# Port Configuration
# ============================================

# Backend API Port (defaults to 3100 if not set)
# Change this if port 3100 is already in use
BACKEND_PORT=3100

# Frontend Web Port (defaults to 3000 if not set)
# Change this if port 3000 is already in use
FRONTEND_PORT=3000

# ============================================
# Frontend API Configuration (Optional)
# ============================================

# For local development, the frontend will automatically use http://localhost:3100
# On Render, the backend host is automatically injected via NEXT_PUBLIC_API_HOST
# Only set this if you need to override the default behavior
# NEXT_PUBLIC_API_URL=http://localhost:3100

# ============================================
# Usage Instructions
# ============================================
#
# 1. Copy this file:
#    cp env.template .env
#
# 2. Edit .env and add your actual API keys
#
# 3. Run with Docker:
#    docker-compose up --build
#
# 4. Access the app:
#    http://localhost:3000
#

